#include "updater.h"

/*Updater::Updater(){}

Updater::~Updater(){}

void Updater::update(double step)
{
    for (size_t i = 0; i < _arr_items.size(); i++)
        _move_sand_items(_arr_items[i], step);
}

void Updater::add_item(const shared_ptr<SandItem>& item)
{
    _arr_items.push_back(item);
}

void Updater::_move_sand_items(shared_ptr<SandItem> &item, double step)
{
    vector<shared_ptr<Vertex>>& v = item->get_model()->v_arr; //&???

    // проверка на выход за границы часов

    for (size_t i = 0; i < v.size(); i++)
        v[i]->y += step;
}*/
